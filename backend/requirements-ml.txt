# --- Dependencias opcionales de Machine Learning --------------------------
# Instalar SOLO en local o en un host con espacio suficiente:
#     pip install -r requirements-ml.txt
#
# Vercel Serverless limita el paquete a ~250 MB descomprimido, por lo que
# TensorFlow NO se despliega alli. Si estas dependencias no estan presentes,
# el backend degrada automaticamente a un clasificador heuristico de respaldo
# (ver app/ml/classifier.py) y lo informa en el campo "engine" de la respuesta.

tensorflow-cpu>=2.18
